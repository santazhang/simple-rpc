namespace test;

// for debugging
struct empty_struct {
};

service Empty {
};

// check if python can handle this properly
struct complex_struct {
    map<string, vector<vector<string>>> d;
    set<string> s;
};

service Math {
    gcd(i64 a, i64 | i64 g);
};
